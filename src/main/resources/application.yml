spring:
  datasource:
    username: zyx
    password: Zyx3527109
    url: jdbc:mysql://localhost:3306/qualityevaluation?serverTimezone=Asia/Shanghai&rewriteBatchedStatements=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 500MB

mybatis-plus:
  mapper-locations: classpath*:/mapper/**
  global-config:
    banner: false
#  configuration:
#    call-setters-on-nulls: false

server:
  port: 8080
